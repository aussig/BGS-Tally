[pytest]
markers =
    live_requests: run the harness with the live requests backend
filterwarnings =
    ignore:Exception ignored while calling deallocator <function Variable.__del__.*:pytest.PytestUnraisableExceptionWarning